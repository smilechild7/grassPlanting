#include <iostream>
#include <string>
using namespace std;

void mergeSort(int start, int mid, int end, string name[N], int score[N][3]){
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int N; cin >> N;
    string name[N];
    int score[N][3];
    for (int i=0; i<N; i++) {
        cin >> name[i] >> score[i][0] >> score[i][1] >> score[i][2];
    }

    for (int i=0; i<N; i++) {

    }

}